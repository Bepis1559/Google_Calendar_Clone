import { type ReactElement } from "react";

export function EditEventModal(props: EditEventModalProps): ReactElement {
  const {
    eventDate,
    eventName,
    isAllDayChecked,
    startTime,
    endTime,
    eventColor,
  } = props;

  return <></>;
}
